
  # SVG Mesh Gradient Generator

  This is a code bundle for SVG Mesh Gradient Generator. The original project is available at https://www.figma.com/design/jfCQrbJQ096mukokar0vrR/SVG-Mesh-Gradient-Generator.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  